# Notebooks

`EEG_Emotion_Recognition.ipynb` is the preserved thesis notebook.

It remains useful as:

- the original research log
- a reference for figures and intermediate experiments
- a source of the reported results already embedded in output cells

The repository should no longer depend on the notebook for basic train/evaluate workflows; use the scripts in `../scripts/` for that.

